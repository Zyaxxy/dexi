/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const USER_ENTRY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  54, 41, 41, 175, 153, 228, 140, 221,
]);

export function getUserEntryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(USER_ENTRY_DISCRIMINATOR);
}

export type UserEntry = {
  discriminator: ReadonlyUint8Array;
  user: Address;
  contest: Address;
  athletes: Array<Address>;
  claimed: boolean;
  /** True once the full 11-player lineup has been validated and staked. */
  isComplete: boolean;
};

export type UserEntryArgs = {
  user: Address;
  contest: Address;
  athletes: Array<Address>;
  claimed: boolean;
  /** True once the full 11-player lineup has been validated and staked. */
  isComplete: boolean;
};

/** Gets the encoder for {@link UserEntryArgs} account data. */
export function getUserEntryEncoder(): FixedSizeEncoder<UserEntryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["user", getAddressEncoder()],
      ["contest", getAddressEncoder()],
      ["athletes", getArrayEncoder(getAddressEncoder(), { size: 11 })],
      ["claimed", getBooleanEncoder()],
      ["isComplete", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: USER_ENTRY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link UserEntry} account data. */
export function getUserEntryDecoder(): FixedSizeDecoder<UserEntry> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["user", getAddressDecoder()],
    ["contest", getAddressDecoder()],
    ["athletes", getArrayDecoder(getAddressDecoder(), { size: 11 })],
    ["claimed", getBooleanDecoder()],
    ["isComplete", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link UserEntry} account data. */
export function getUserEntryCodec(): FixedSizeCodec<UserEntryArgs, UserEntry> {
  return combineCodec(getUserEntryEncoder(), getUserEntryDecoder());
}

export function decodeUserEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserEntry, TAddress>;
export function decodeUserEntry<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserEntry, TAddress>;
export function decodeUserEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<UserEntry, TAddress> | MaybeAccount<UserEntry, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserEntryDecoder(),
  );
}

export async function fetchUserEntry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserEntry, TAddress>> {
  const maybeAccount = await fetchMaybeUserEntry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserEntry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserEntry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserEntry(maybeAccount);
}

export async function fetchAllUserEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserEntry>[]> {
  const maybeAccounts = await fetchAllMaybeUserEntry(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserEntry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeUserEntry(maybeAccount));
}

export function getUserEntrySize(): number {
  return 426;
}
