/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { DEXI_PROGRAM_ADDRESS } from "../programs";
import {
  getAthleteRoleDecoder,
  getAthleteRoleEncoder,
  type AthleteRole,
  type AthleteRoleArgs,
} from "../types";

export const UPDATE_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  239, 214, 170, 78, 36, 35, 30, 34,
]);

export function getUpdatePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_POOL_DISCRIMINATOR);
}

export type UpdatePoolInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: Option<string>;
  role: Option<AthleteRole>;
  enabled: Option<boolean>;
};

export type UpdatePoolInstructionDataArgs = {
  name: OptionOrNullable<string>;
  role: OptionOrNullable<AthleteRoleArgs>;
  enabled: OptionOrNullable<boolean>;
};

export function getUpdatePoolInstructionDataEncoder(): Encoder<UpdatePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "name",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["role", getOptionEncoder(getAthleteRoleEncoder())],
      ["enabled", getOptionEncoder(getBooleanEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_POOL_DISCRIMINATOR }),
  );
}

export function getUpdatePoolInstructionDataDecoder(): Decoder<UpdatePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "name",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["role", getOptionDecoder(getAthleteRoleDecoder())],
    ["enabled", getOptionDecoder(getBooleanDecoder())],
  ]);
}

export function getUpdatePoolInstructionDataCodec(): Codec<
  UpdatePoolInstructionDataArgs,
  UpdatePoolInstructionData
> {
  return combineCodec(
    getUpdatePoolInstructionDataEncoder(),
    getUpdatePoolInstructionDataDecoder(),
  );
}

export type UpdatePoolAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  admin: TransactionSigner<TAccountAdmin>;
  name: UpdatePoolInstructionDataArgs["name"];
  role: UpdatePoolInstructionDataArgs["role"];
  enabled: UpdatePoolInstructionDataArgs["enabled"];
};

export async function getUpdatePoolInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: UpdatePoolAsyncInput<TAccountConfig, TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdatePoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountAdmin
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getUpdatePoolInstructionDataEncoder().encode(
      args as UpdatePoolInstructionDataArgs,
    ),
    programAddress,
  } as UpdatePoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountAdmin
  >);
}

export type UpdatePoolInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  admin: TransactionSigner<TAccountAdmin>;
  name: UpdatePoolInstructionDataArgs["name"];
  role: UpdatePoolInstructionDataArgs["role"];
  enabled: UpdatePoolInstructionDataArgs["enabled"];
};

export function getUpdatePoolInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: UpdatePoolInput<TAccountConfig, TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdatePoolInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getUpdatePoolInstructionDataEncoder().encode(
      args as UpdatePoolInstructionDataArgs,
    ),
    programAddress,
  } as UpdatePoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountAdmin
  >);
}

export type ParsedUpdatePoolInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    admin: TAccountMetas[2];
  };
  data: UpdatePoolInstructionData;
};

export function parseUpdatePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdatePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      admin: getNextAccount(),
    },
    data: getUpdatePoolInstructionDataDecoder().decode(instruction.data),
  };
}
