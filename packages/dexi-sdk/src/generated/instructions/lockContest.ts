/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { DEXI_PROGRAM_ADDRESS } from "../programs";

export const LOCK_CONTEST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  124, 155, 70, 224, 136, 196, 104, 207,
]);

export function getLockContestDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOCK_CONTEST_DISCRIMINATOR,
  );
}

export type LockContestInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type LockContestInstructionData = { discriminator: ReadonlyUint8Array };

export type LockContestInstructionDataArgs = {};

export function getLockContestInstructionDataEncoder(): FixedSizeEncoder<LockContestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LOCK_CONTEST_DISCRIMINATOR }),
  );
}

export function getLockContestInstructionDataDecoder(): FixedSizeDecoder<LockContestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLockContestInstructionDataCodec(): FixedSizeCodec<
  LockContestInstructionDataArgs,
  LockContestInstructionData
> {
  return combineCodec(
    getLockContestInstructionDataEncoder(),
    getLockContestInstructionDataDecoder(),
  );
}

export type LockContestAsyncInput<
  TAccountConfig extends string = string,
  TAccountContest extends string = string,
  TAccountKeeper extends string = string,
> = {
  config?: Address<TAccountConfig>;
  contest: Address<TAccountContest>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export async function getLockContestInstructionAsync<
  TAccountConfig extends string,
  TAccountContest extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: LockContestAsyncInput<TAccountConfig, TAccountContest, TAccountKeeper>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LockContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountKeeper
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("contest", accounts.contest),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getLockContestInstructionDataEncoder().encode({}),
    programAddress,
  } as LockContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountKeeper
  >);
}

export type LockContestInput<
  TAccountConfig extends string = string,
  TAccountContest extends string = string,
  TAccountKeeper extends string = string,
> = {
  config: Address<TAccountConfig>;
  contest: Address<TAccountContest>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export function getLockContestInstruction<
  TAccountConfig extends string,
  TAccountContest extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: LockContestInput<TAccountConfig, TAccountContest, TAccountKeeper>,
  config?: { programAddress?: TProgramAddress },
): LockContestInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountContest,
  TAccountKeeper
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("contest", accounts.contest),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getLockContestInstructionDataEncoder().encode({}),
    programAddress,
  } as LockContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountKeeper
  >);
}

export type ParsedLockContestInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    contest: TAccountMetas[1];
    keeper: TAccountMetas[2];
  };
  data: LockContestInstructionData;
};

export function parseLockContestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLockContestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      contest: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getLockContestInstructionDataDecoder().decode(instruction.data),
  };
}
