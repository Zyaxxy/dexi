/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { DEXI_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_CONTEST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  79, 122, 33, 192, 110, 98, 219, 238,
]);

export function getSettleContestDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_CONTEST_DISCRIMINATOR,
  );
}

export type SettleContestInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountContest extends string | AccountMeta<string> = string,
  TAccountEscrowVault extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountContest extends string
        ? WritableAccount<TAccountContest>
        : TAccountContest,
      TAccountEscrowVault extends string
        ? WritableAccount<TAccountEscrowVault>
        : TAccountEscrowVault,
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type SettleContestInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SettleContestInstructionDataArgs = {};

export function getSettleContestInstructionDataEncoder(): FixedSizeEncoder<SettleContestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SETTLE_CONTEST_DISCRIMINATOR }),
  );
}

export function getSettleContestInstructionDataDecoder(): FixedSizeDecoder<SettleContestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSettleContestInstructionDataCodec(): FixedSizeCodec<
  SettleContestInstructionDataArgs,
  SettleContestInstructionData
> {
  return combineCodec(
    getSettleContestInstructionDataEncoder(),
    getSettleContestInstructionDataDecoder(),
  );
}

export type SettleContestAsyncInput<
  TAccountConfig extends string = string,
  TAccountContest extends string = string,
  TAccountEscrowVault extends string = string,
  TAccountKeeper extends string = string,
> = {
  config?: Address<TAccountConfig>;
  contest: Address<TAccountContest>;
  escrowVault: Address<TAccountEscrowVault>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export async function getSettleContestInstructionAsync<
  TAccountConfig extends string,
  TAccountContest extends string,
  TAccountEscrowVault extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: SettleContestAsyncInput<
    TAccountConfig,
    TAccountContest,
    TAccountEscrowVault,
    TAccountKeeper
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountEscrowVault,
    TAccountKeeper
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: true },
    escrowVault: { value: input.escrowVault ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("contest", accounts.contest),
      getAccountMeta("escrowVault", accounts.escrowVault),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getSettleContestInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountEscrowVault,
    TAccountKeeper
  >);
}

export type SettleContestInput<
  TAccountConfig extends string = string,
  TAccountContest extends string = string,
  TAccountEscrowVault extends string = string,
  TAccountKeeper extends string = string,
> = {
  config: Address<TAccountConfig>;
  contest: Address<TAccountContest>;
  escrowVault: Address<TAccountEscrowVault>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export function getSettleContestInstruction<
  TAccountConfig extends string,
  TAccountContest extends string,
  TAccountEscrowVault extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof DEXI_PROGRAM_ADDRESS,
>(
  input: SettleContestInput<
    TAccountConfig,
    TAccountContest,
    TAccountEscrowVault,
    TAccountKeeper
  >,
  config?: { programAddress?: TProgramAddress },
): SettleContestInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountContest,
  TAccountEscrowVault,
  TAccountKeeper
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEXI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    contest: { value: input.contest ?? null, isWritable: true },
    escrowVault: { value: input.escrowVault ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("contest", accounts.contest),
      getAccountMeta("escrowVault", accounts.escrowVault),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getSettleContestInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleContestInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountContest,
    TAccountEscrowVault,
    TAccountKeeper
  >);
}

export type ParsedSettleContestInstruction<
  TProgram extends string = typeof DEXI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    contest: TAccountMetas[1];
    escrowVault: TAccountMetas[2];
    keeper: TAccountMetas[3];
  };
  data: SettleContestInstructionData;
};

export function parseSettleContestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleContestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      contest: getNextAccount(),
      escrowVault: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getSettleContestInstructionDataDecoder().decode(instruction.data),
  };
}
